version: "2"
sql:
  - engine: "postgresql"
    queries: "internal/data/queries/queries.sql"
    schema: "internal/data/schema/schema.sql"
    gen:
      go:
        package: "db"
        out: "internal/data/db"
