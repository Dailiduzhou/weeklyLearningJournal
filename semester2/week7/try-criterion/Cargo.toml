[package]
name = "try-criterion"
version = "0.1.0"
edition = "2024"

[dependencies]

[dev-dependencies]
criterion = { version = "0.8.2", features = ["html_reports"]}

[[bench]]
name = "my_benchmarks"
harness = false
