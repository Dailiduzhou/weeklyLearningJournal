syntax = "proto3";

package post;
option go_package="./post";

message GetpostReq {
  int64 userid = 1;
}

message GetpostResp {
  message Post {
    int64 id = 1;
    string name = 2;
  };
  repeated Post posts = 1;
}

service Post {
  rpc Getpost(GetpostReq) returns (GetpostResp);
}
