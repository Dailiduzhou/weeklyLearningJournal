[package]
name = "rmcp-client"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = {version = "1.52", features = ["full"]}
rmcp = {version = "1.6.0", features = ["client", "transport-child-process"]}
